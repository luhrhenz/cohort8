Grouping related data
A struct bundles related fields under one name — like a row in a database table, but typed. Each field has an explicit type: no surprises.

name: `String` lives on the heap. id, age, score live on the stack.